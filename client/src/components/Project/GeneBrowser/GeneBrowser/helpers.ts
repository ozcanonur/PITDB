// export const parseDiscreteSliderMarks = (marks: string[]) =>
//   marks.map((mark, index) => ({
//     value: index,
//     scaledValue: parseFloat(mark),
//     label: mark,
//   }));

export {};
